apply plugin: "java"

sourceCompatibility = "16"
group = "it.polimi.saefa"
version "latest"

tasks.named("jar") {
    manifest {
        attributes("Implementation-Title": project.name, "Implementation-Version": project.version)
    }
}